# Code Style Preferences

- Prefer `type` over `interface` for TypeScript type definitions
- Use snake_case for file names (e.g., `my_module.ts` not `my-module.ts`)
- Use snake_case for variable and function names (e.g., `board_state` not `boardState`)
